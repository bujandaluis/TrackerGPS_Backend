PORT=3088
HOST=0.0.0.0
NODE_ENV=development
DATABASE_PATH=./data/trackergps.db

# En Render: PORT lo asigna la plataforma automáticamente.
# Usa NODE_ENV=production y DATABASE_PATH=./data/trackergps.db (ver render.yaml).
